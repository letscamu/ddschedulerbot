name: Deploy to Cloud Run

on:
  push:
    branches: [master]

# Only allow one deployment at a time
concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        env:
          FLASK_ENV: testing
          USE_LOCAL_STORAGE: "true"
          SECRET_KEY: test-secret
          ADMIN_USERNAME: admin
          ADMIN_PASSWORD: admin
        run: pytest tests/ -v

  deploy:
    name: Deploy to Google Cloud Run
    needs: test
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write  # Required for Workload Identity Federation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Create prod env.yaml
        run: |
          cat > env-prod.yaml << 'ENVEOF'
          FLASK_ENV: "production"
          USE_LOCAL_STORAGE: "false"
          GCS_BUCKET: "ddschedulerbot-files"
          SECRET_KEY: "${{ secrets.SECRET_KEY }}"
          ADMIN_USERNAME: "${{ secrets.ADMIN_USERNAME }}"
          ADMIN_PASSWORD: "${{ secrets.ADMIN_PASSWORD }}"
          USERS: "${{ secrets.USERS }}"
          BEHIND_PROXY: "true"
          ENVEOF

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy estradabot \
            --source . \
            --region us-central1 \
            --allow-unauthenticated \
            --project ${{ secrets.GCP_PROJECT_ID }} \
            --env-vars-file env-prod.yaml \
            --service-account=969733401480-compute@developer.gserviceaccount.com \
            --quiet

      - name: Verify deployment
        run: |
          echo "Deployment complete. Verifying service is healthy..."
          SERVICE_URL=$(gcloud run services describe estradabot \
            --region us-central1 \
            --project ${{ secrets.GCP_PROJECT_ID }} \
            --format='value(status.url)')
          echo "Service URL: $SERVICE_URL"
          # Wait for service to be ready, then check health
          sleep 10
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$SERVICE_URL")
          if [ "$HTTP_STATUS" -eq 200 ] || [ "$HTTP_STATUS" -eq 302 ]; then
            echo "Service is healthy (HTTP $HTTP_STATUS)"
          else
            echo "WARNING: Service returned HTTP $HTTP_STATUS"
            exit 1
          fi
