{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--browser", "chromium",
        "--executable-path", "/root/.cache/ms-playwright/chromium-1194/chrome-linux/chrome"
      ]
    }
  }
}
